{
  "name": "@pos/web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Restaurant POS frontend — Solid + Vite + Material Design 3, deployed on Cloudflare Pages.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "deploy": "npm run build && wrangler pages deploy dist --project-name restaurant-pos",
    "icons": "node scripts/generate-icons.mjs"
  },
  "dependencies": {
    "@material/web": "^2.4.0",
    "@pos/shared": "*",
    "@solidjs/router": "^0.15.0",
    "@tanstack/solid-query": "^5.62.0",
    "solid-js": "^1.9.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.14",
    "vite-plugin-solid": "^2.11.0",
    "wrangler": "^4.40.0"
  }
}
